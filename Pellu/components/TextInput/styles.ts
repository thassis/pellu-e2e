import { StyleSheet } from 'react-native';
import Colors from '../../utils/Colors';

export default StyleSheet.create({
  container: {
    marginVertical: 4,
    paddingHorizontal: 4,
    width: '100%',
  },
  errorText: {
    color: Colors.error,
    fontSize: 14,
    marginTop: 4,
  }
});
