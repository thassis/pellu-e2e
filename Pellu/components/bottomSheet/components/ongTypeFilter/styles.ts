import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  container: {
    flexDirection: 'row',
    gap: 8,
    marginBottom: 16,
    width: '100%',
    flexWrap: 'wrap',
    flex: 1,
    marginTop: 16,
    flexGrow: 1,
  },
});
